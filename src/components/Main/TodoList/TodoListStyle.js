import {makeStyles} from "@material-ui/core/styles";

const useStyles  = makeStyles ({
    formControl: {
        margin: "10px",
        minWidth: 120,
    },
      selectEmpty: {
        marginTop: "10px",
    },
    emptyListTitle: {
        textAlign: "center",
        marginTop: "70px",
    }
})

export default useStyles;