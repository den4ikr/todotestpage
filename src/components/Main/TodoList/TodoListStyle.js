import {makeStyles} from "@material-ui/core/styles";

const useStyles  = makeStyles ({
    formControl: {
        margin: "10px",
        minWidth: 120,
    },
      selectEmpty: {
        marginTop: "10px",
    },
    input: {
        marginLeft: "10px",
        marginRight: "10px",
    },
})

export default useStyles;